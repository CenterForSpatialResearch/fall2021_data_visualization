//move to HTML within script
{/* 
<script>
Promise.all([d3.csv(""), d3.csv("")])
.then(function(data){
    drawChart(data[0])
    drawChart2(data[1])
})
</script> 
*/}

// create function after loading the data -- name it whatever (ex. drawChart)
// call data in function

function drawChart(data){
    // add d3 code for chart
    var svg = d3.select("#chart")

}

function drawChart2(data){
    // add d3 code for chart
    var svg = d3.select("#chart2")

}
